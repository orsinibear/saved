{
  "name": "smart-contract",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "clean": "rimraf artifacts cache types dist || true",
    "build": "hardhat compile",
    "test": "hardhat test",
    "deploy:alfajores": "hardhat ignition deploy ignition/modules/Factory.ts --network alfajores"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ignition": "^3.0.5",
    "@nomicfoundation/hardhat-toolbox-viem": "^5.0.1",
    "@types/node": "^22.19.1",
    "forge-std": "github:foundry-rs/forge-std#v1.9.4",
    "hardhat": "^3.0.16",
    "typescript": "~5.8.0",
    "viem": "^2.40.3"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.2",
    "dotenv": "^16.4.5",
    "rimraf": "^6.0.1"
  }
}
